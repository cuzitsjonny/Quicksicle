using System;

namespace Quicksicle.Enums
{
    public enum RemoteConnectionType : ushort
    {
        General = 0,
        Auth = 1,
        Chat = 2,
        // ? = 3
        World = 4,
        Client = 5
    }
}
